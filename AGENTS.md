# 项目协作规则

- 先检查错误前提、逻辑跳跃和关键信息缺失；独立判断，区分事实、推测与产品建议。
- 涉及重要结论和外部产品能力时核查来源与适用版本。公开投诉不能替代代表性用户研究。
- 按 `docs/剪贴板应用-PRD.md` 的三阶段流程开发。第三阶段 0.3.1 预览版已合入 main；基线 Windows 11、macOS 14 起（Intel / Apple Silicon）、Ubuntu 22.04 X11。真实截图、微信图片粘贴和登录自启动仍待验收，不把未测试的平台标记为支持。
- 用户要求可直接获取交付物：已验证的阶段版本合入 main，并发布含安装包、校验文件与对应源码的 GitHub Release；尚未满足稳定版出口时标记为预览版。不能仅推开发分支或提供会过期的 Actions Artifacts 就视为交付完成。
- 保持简洁；暂不加入账号、云同步、OCR、复杂分类和插件体系。
- `data/` 是用户的真实剪贴板历史。不得读取、输出、测试或提交其内容；测试使用临时目录与自造数据。
- 本机剪贴板及按键自动化只能使用独立 X11 服务器。Windows/macOS 原生集成测试只在 GitHub 托管的临时 runner 上运行，不连接用户桌面。测试开关 `CLIPBOARD_ISOLATED_TEST=1` 不是隔离本身，必须配合 Xephyr / Xvfb 等独立显示服务器。
- 所有测试数据、数据库、运行锁、密钥、本机专用启动器和个人路径均不得提交。图片预览必须使用合成示例。
- 验证正常路径和关键失败路径。粘贴完成以目标收到内容为准，不能只检查按键是否已发送。
- 对来源识别、终端行为、输入法和平台兼容性保留已知限制；不要通过假设扩大支持承诺。
- 本项目采用 GPL-3.0-only；引入依赖时保留许可证信息并检查兼容性。

第三阶段预览的三端自动化与安装包检查已通过，记录见 `docs/第三阶段验收报告.md`。正式签名、Windows 11 / Mac 基线实机及常用应用验收仍未完成；保留工程预览标记，不把 CI 成功扩大为全面兼容。

用户要求简洁且好看，已指出文字裁切。后续界面改动须检查中文、混排、窄窗口、较大字体与高 DPI，使用实际 Qt 渲染和合成记录；不能仅换配色或扩大固定尺寸后就宣称修复。0.3.1 的设计与验证记录见 `docs/界面改版验收报告.md`。

2026-09-16：用户确认 0.4.0 需求：日期分组、500 MiB 默认预算、单条自动受总容量约束、1/7/30/365 天与无限期五档（默认 30 天）、默认登录启动且可关闭、行尾黄色收藏星、优先保留原始图片字节、磁盘与运行内存分别展示和清理。不得通过“清理内存”删除历史。PRD v0.9；验收见 docs/历史管理验收报告.md。测试的自启动配置也必须使用临时目录。

0.4.1 设置页回归：顶层对话框不自动继承主面板字体。后续所有对话框布局验收须覆盖实际中文 fallback 字形、内部编辑区域、长选项宽度、窄窗口滚动与固定操作区，不能只检查主面板或名义 fontMetrics.height。设置修复报告见 docs/设置窗口修复验收.md。


2026-09-24：用户明确要求可自定义历史保存文件夹，并报告在 Codex 使用 Ctrl+Alt+V 后自动粘贴失败。0.5.0 增加保存目录选择、复制校验、回退与重启；原目录保留备份，不能静默覆盖已有历史或在数据盘离线时创建空库。测试的目录偏好与启动项必须指向临时位置。后台可见面板重新捕获外部目标，焦点与修饰键超时分别提示；不得把当前只读窗口元数据正常扩大为 Codex 实际粘贴已全部验收。详见目录与粘贴验收报告。

2026-09-25：用户确认以 Raycast 为主要视觉参考。0.6.0 整理紧凑搜索列表、按需自适应预览、三页设置及弹窗，PRD v0.12；验收见 docs/Raycast方向界面验收.md。关闭或隐藏预览时释放内容；大文字和大图片先显示摘要并提供完整查看入口，存档与复制保持完整。所有 UI 测试使用合成数据，覆盖中文、窄窗、大字号、高缩放、原生滚动条宽度和真实计时器事件；本机已正常更新。三端 CI 及四安装包检查通过，保留 Codex／常用应用与实机验收限制。
