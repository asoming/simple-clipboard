# 浅色极简界面验收（0.7.0）

日期：2026-09-25
状态：本机隔离回归、字体缩放、合成界面、三端构建与四种安装包检查通过，本机已更新；公开预览已发布并完成九附件校验，实机验收继续保留。

## 范围与依据

用户提供了搜索、紧凑左侧列表、右侧预览的参考图，并明确要求减少无用文字、继续使用浅色。本轮按这个已确认方向重新安排主面板，保留字体可读性、收藏、日期分组和底层数据行为。需求基线为 [PRD v0.13](剪贴板应用-PRD.md) 第 15 节。

将文字收进菜单或提示是界面判断，不代表已证明效率提升。参考图中的来源应用、复制次数等信息没有可靠数据时不展示，也不增加账号、云同步或 OCR。

## 验证结果

| 项目 | 验证方式 | 当前结果 |
|---|---|---|
| 浅色分栏与精简文字 | 合成文字、图片、跨日记录截图，与用户参考对照 | 已完成本轮 Qt 渲染及目视检查，见下方截图 |
| 搜索、筛选与列表 | 类型／收藏、日期分组、单行省略、键盘选择、行尾星标 | 通过本轮合成／隔离检查 |
| 内容预览 | 默认展开、宽左右／窄上下、开关、选择联动、大内容与完整查看 | 通过本轮合成／隔离检查 |
| 菜单与反馈 | 图标可达性、悬浮提示、暂停／忽略、复制、粘贴与失败反馈 | 通过本轮合成／隔离检查 |
| 字体与布局 | 中文混排、窄窗口、大字体、高 DPI、浅深色及各弹窗 | 通过本轮合成／隔离检查 |
| 粘贴与存储回归 | 隔离桌面、临时数据、原图、保存规则、目录迁移和失败路径 | 通过本轮合成／隔离检查 |
| 三端自动化与安装包 | 对应提交的 GitHub Actions 和四种包验证报告 | 通过，见下方 CI 记录 |
| 本机更新 | 正常退出与启动，仅核对进程、窗口标识 | 已正常退出旧版并通过现有启动器运行 0.7.0 |
| 公开预览发布 | main、四种安装包、三份源码、校验与验证记录 | 已公开，九个附件与四个匿名下载链接检查通过 |

## 本轮实现范围

- 搜索与类型筛选合为一行，移除重复标题、普通记录状态、数量与整行快捷键说明。
- 左侧单行列表保留日期组和行尾收藏星；长摘要明确省略，选中内容在右侧查看。
- 默认展开预览，新布局记住自身开关偏好。内容与实际元信息分清主次，窄窗改为上下布局。
- 底部用更多、预览、复制图标和格式选择减少文字，保留一个主要「粘贴」按钮；菜单与悬浮提示提供次要操作和快捷键。
- 既有外观选择保留，以浅色作为本轮目视验收的主要结果；不修改用户已保存的外观选择。
- 普通成功反馈简短；暂停、粘贴失败、存储错误和删除确认仍明确展示。

### 与上一版的变化

| 项目 | 0.6.0 | 0.7.0 |
|---|---|---|
| 主面板结构 | 标题／状态、搜索、四个筛选按钮、列表、操作与快捷键说明 | 一行搜索与类型选择、内容分栏、轻量底部操作 |
| 列表摘要 | 两行正文，加时间与类型说明 | 单行摘要，保留日期分组、内容图标和收藏星 |
| 预览 | 默认按需打开 | 新布局默认展开并记住开关，宽左右／窄上下 |
| 大内容保护 | 文字超过 1 MiB、图片超过 16 MiB 显示摘要 | 额外限制 超过 20,000 字符的文本，防止长单行导航时同步布局 |
| 独立完整长文本 | 自动折行 | 长文本横向滚动，完整内容保留 |

以上是可检查的界面与行为变化；不据此推断使用效率或自动粘贴成功率提升。

## 验证记录要求

应用版本：`0.7.0`；预览标签：`v0.7.0-preview.1`。Release 应含四种安装包、三份依赖对应源码、`SHA256SUMS` 和 `verification.json`，后者分别记录测试构建提交与发行文档提交。

所有截图仅使用合成记录，不读取或公开真实剪贴板历史。本机剪贴板和按键测试只能在独立 X11 显示服务器进行，设置、数据目录和自启动测试使用临时位置。补充实际命令、环境、数量和提交后才标记完成。

## 本机验证记录

- Ubuntu 22.04.5 LTS／GNOME／X11，系统 Python 3.10 与 PyQt5；新建独立显示服务器 `:125`，不连接实际桌面剪贴板。
- 全量回归 **159 项，150 通过，9 项跨平台测试跳过**，耗时 22.643 秒。命令：`CLIPBOARD_ISOLATED_TEST=1 QT_QPA_PLATFORM=xcb python3 -m unittest discover -s tests -v`，DISPLAY 指向独立服务器。
- 主面板、预览、设置和小弹窗布局：**150%＋120 DPI 与 200%＋120 DPI 各 39 项全部通过**。使用合成记录覆盖浅深色、窄窗、中文混排与较大字体；主面板测试包含 10／16／20pt，预览和弹窗另覆盖至 26pt。
- 回归覆盖搜索／类型筛选、单行省略、日期与星标、默认预览和开关偏好、内容联动、较大内容按需读取、复制／粘贴快捷键及暂停／忽略状态。设置、目录迁移、原图和终端保护继续通过临时数据或隔离桌面检查。
- 本轮发现并修复高缩放、大字号下类型选择的文字空间不足：按实际样式的内容区和中文字形计算尺寸；修正后两个缩放组均通过。
- 增加字符数保护和长文本横向滚动，覆盖长单行 ASCII 文本，避免默认预览导航时隐式加载并折行大内容；复制、粘贴仍使用完整原记录。
- 已刷新并目视检查实际 Qt 浅色文字分栏（820×530）、图片预览、窄窗上下布局（451×680）及三页设置（600×560）。窄窗转换后可继续缩到 440；尺寸以逻辑像素记录。全部使用合成数据，没有读取或公开用户真实历史。

本机更新：2026-09-25，通过旧版自身菜单正常退出，并确认旧进程结束；再以现有 `start.sh` 启动 0.7.0。新窗口为 820×530 逻辑像素（本机 200% 缩放下 1640×1060 物理像素）。仅核对进程、启动命令与窗口元数据，没有读取真实历史或截图真实主界面，也未迁移保存目录或改动自启动选项。三端 CI 与公开交付仍分别以各自记录为准。

## 合成截图

![浅色文字分栏](images/minimal-main-light.png)

![浅色图片预览](images/minimal-image-light.png)

![窄窗口上下布局](images/minimal-narrow-light.png)

## 延续的验收边界

保留 500 MiB 默认容量、五档期限、收藏保护、原图、目录迁移和粘贴焦点约束。预览关闭与隐藏时释放内容。文本超过 20,000 字符或 1 MiB、图片超过 16 MiB 先显示摘要，明确打开后才加载完整内容；这些阈值控制预览成本，不限制已保存或复制的内容。完整长文本使用横向滚动，保存与复制保持完整原内容。

Codex 间歇粘贴、微信图片、日常截图、Windows 11、Mac Intel 的 macOS 14 基线、常用应用中文输入法、首次权限授权、睡眠恢复和真实登录自启动仍需对应使用场景验收。Windows 发布者签名和 macOS Developer ID 公证尚未完成，继续按工程预览发布。CI 成功不替代这些项目，Wayland 与其他 Linux 桌面不在已验证范围。

<!-- BEGIN CI 0.7.0 -->
## 三端自动化与安装包检查

[GitHub Actions 验证](https://github.com/asoming/simple-clipboard/actions/runs/36120527624)，应用及构建来源提交 `64498089de79dd03cdf90aff9a6408c6c980f563`。

| 平台 | 总计 | 通过 | 跳过 |
|---|---:|---:|---:|
| linux-x11 | 159 | 150 | 9 |
| native (windows-2025, windows-x64) | 159 | 132 | 27 |
| native (macos-14, macos-arm64) | 159 | 134 | 25 |
| native (macos-15-intel, macos-intel) | 159 | 134 | 25 |

Linux deb 安装、重装与卸载检查通过；Windows 可执行文件启动、安装、升级与数据保留检查通过；两种 Mac 包启动、签名结构检查、dmg 挂载与复制启动通过。构建同时收集对应源码及许可证。Mac 的签名结构检查不等于 Developer ID 签名或公证。

以上 CI 结果适用于对应的临时测试环境。本机更新另见上文；公开 Release 附件校验和发布已完成，见下节。自动化不替代 Codex、微信图片、Windows 11／Mac 用户设备、真实登录、首次授权或睡眠恢复验收。
<!-- END CI 0.7.0 -->

## 公开交付验证

[0.7.0 工程预览 Release](https://github.com/asoming/simple-clipboard/releases/tag/v0.7.0-preview.1) 已公开，包含四种安装包、三份对应源码、`SHA256SUMS` 与 `verification.json`，共九个附件。全部附件的 GitHub SHA-256 与校验清单一致；四个本地安装包大小与 SHA-256 均一致，四条安装包链接匿名请求均返回 HTTP 200。

应用及安装包来自提交 `64498089de79dd03cdf90aff9a6408c6c980f563`；发行标签固定在 `ffbcff77c62974dee549b2d434a1801c03c4d2c3`，两者之间只含文档与验证记录变更。发布后的本报告与 PRD 完成状态通过单独文档提交写入 main，应用代码仍与已测试的构建提交一致。

[附件传输与校验流程](https://github.com/asoming/simple-clipboard/actions/runs/36120987562) 成功后才公开；本地交付副本位于 `outputs/minimal-light-preview/`（工作区交付目录，不随源码入库）。本轮 UI 迭代与工程预览交付完成；上文实机及签名限制继续保留。
